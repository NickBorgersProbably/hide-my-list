name: Build and Push Docker Image

# Disabled — no automated image push configured.
# The app image is built locally via docker compose (see docker/Dockerfile).
# Retained for potential future use.

on:
  workflow_dispatch:

jobs:
  placeholder:
    name: Docker Build (No-op)
    runs-on: ubuntu-latest
    steps:
      - name: Skip
        run: echo "No application Dockerfile — this is an OpenClaw agent project"
