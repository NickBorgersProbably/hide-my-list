name: Build and Push Docker Image

# Disabled — this is an OpenClaw agent project with no application Dockerfile.
# The devcontainer image is built by the review pipeline, not this workflow.
# Retained for potential future use.

on:
  workflow_dispatch:

jobs:
  placeholder:
    name: Docker Build (No-op)
    runs-on: ubuntu-latest
    steps:
      - name: Skip
        run: echo "No application Dockerfile — this is an OpenClaw agent project"
